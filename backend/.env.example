# Copy this file to .env and fill in your key to enable real AI generation.
# Without a key, the app automatically uses a rule-based fallback so the
# demo still works (useful when there's no internet during the live demo).
ANTHROPIC_API_KEY=your_key_here
ANTHROPIC_MODEL=claude-sonnet-4-6
